# Halal Formosa Project Rules

## Development Constraints
- **No Automatic Builds**: Do not run `npm run build`, `vite build`, or other build/bundling commands unless the user explicitly requests you to build the project.
